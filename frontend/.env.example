# The frontend talks to the backend via Vite's dev proxy (see vite.config.js),
# which forwards /api and /uploads to http://localhost:5000.
# No frontend env vars are required for local development.
#
# For production, set your deployed API's base URL and update
# src/api/axios.js's baseURL accordingly (or use an env var like VITE_API_URL).
